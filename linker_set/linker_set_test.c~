#include <stdio.h>
#include "linker_set.h"

int
main(int argc, char **argv)
{
	int **i, sum;
	LINKER_SET_DECLARE(tset, int);

	printf("%p -> %p\n", LINKER_SET_START(tset), LINKER_SET_END(tset));
	sum = 0;
	i = LINKER_SET_START(tset);
	fprintf(stderr, "%p, %p\n", i, *i);
	LINKER_SET_FOREACH(i, tset) {
		printf("%p\n", i);
		sum += **i;
	}

	printf("%d\n", sum);

	return sum != 50;
}
